# Rooms / Layer 2 placeholders
# Copy the values you really use into the live .env of the stack; never commit real secrets.

ROOMS_BIND_HOST=0.0.0.0
ROOMS_SECRETS_FILE=/data/rooms-secrets.json
ROOMS_DEFAULT_ROOM=ROOMS_LAB
